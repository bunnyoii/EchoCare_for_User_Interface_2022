#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/WeChatProjects/EchoCare/node_modules/.store/qrcode@1.5.3/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/WeChatProjects/EchoCare/node_modules/.store/qrcode@1.5.3/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/qrcode@1.5.3/node_modules/qrcode/bin/qrcode" "$@"
else
  exec node  "$basedir/../.store/qrcode@1.5.3/node_modules/qrcode/bin/qrcode" "$@"
fi
